use world;

show tables;


create table member (
    seq int,
    name varchar(100),
    id varchar(100)
    regdate datetime
);

create table member (
    seq int,
    name varchar(100),
    id varchar(100)
    regdate datetime
);
